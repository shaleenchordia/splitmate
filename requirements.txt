# Vercel's Python builder installs from the repo-root requirements.txt.
# Keep in sync with backend/requirements.txt (gunicorn omitted: Vercel
# provides the server).
Django==5.1.11
djangorestframework==3.16.0
django-cors-headers==4.7.0
openpyxl==3.1.5
whitenoise==6.9.0
dj-database-url==2.3.0
psycopg[binary]==3.2.9
